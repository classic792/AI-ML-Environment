The is the requirement for setting up an AI/ML Environmnet.
These are the commands you should follow to install it:
git clone https://github.com/classic792/AI-ML-Environment.git
cd AI-ML-Environment
python -m venv venv
venv\Scripts\activate  # or source venv/bin/activate on Mac/Linux
pip install -r requirements.txt
